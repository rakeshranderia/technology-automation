# 01 — Linux Access and Administration

Focus areas include directory integration, privileged access, package management and repeatable configuration.

A useful pattern is:

**Identity → Privilege → Configuration → Logging → Review**

Example experience patterns include joining Linux hosts to directory services and defining controlled sudo access.

Evidence may include configuration files, command history, access records and validation output.
