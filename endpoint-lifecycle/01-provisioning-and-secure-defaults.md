# 01 — Provisioning and Secure Defaults

New endpoints should begin from a known baseline.

Consider:

- device identity;
- encryption;
- endpoint protection;
- patching;
- configuration policy;
- least privilege;
- remote management.

Modern tooling may include Intune and Autopilot, but the control objective is consistent provisioning rather than a specific product.
